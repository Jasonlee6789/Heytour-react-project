import React from "react";

const SelectForm = () => {
  return (
    <form>
      <input type="text" name="filterTerm" />
      <button>Submit</button>
    </form>
  );
};

export default SelectForm;
